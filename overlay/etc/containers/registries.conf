# Image registries for Mica OS.

unqualified-search-registries = ["docker.io"]

# "enforcing" fails every short-name pull without a TTY, which is every
# Quadlet-generated unit.
short-name-mode = "disabled"
