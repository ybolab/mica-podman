# Container storage. /mica/containers is the DATA/containers bind with its own
# project accounting; runtime state stays on /run.
[storage]
driver = "overlay"
graphroot = "/mica/containers/storage"
runroot = "/run/containers/storage"

[storage.options]
[storage.options.overlay]
mountopt = "nodev"
